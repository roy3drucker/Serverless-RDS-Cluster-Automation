version: 2.1

orbs:
  aws-cli: circleci/aws-cli@4.1.3

commands:
  install_sam:
    steps:
      - run:
          name: Install AWS SAM CLI
          command: |
            python -m pip install --upgrade pip
            pip install aws-sam-cli==1.116.0
  install_terraform:
    steps:
      - run:
          name: Install Terraform 1.7.x
          command: |
            sudo apt-get update
            sudo apt-get install -y unzip curl
            curl -fsSL https://releases.hashicorp.com/terraform/1.7.5/terraform_1.7.5_linux_amd64.zip -o /tmp/terraform.zip
            sudo unzip -o /tmp/terraform.zip -d /usr/local/bin
            terraform version

executors:
  python-executor:
    docker:
      - image: cimg/python:3.11
    working_directory: ~/project

jobs:
  validate:
    executor: python-executor
    steps:
      - checkout
      - install_sam
      - install_terraform
      - run:
          name: Validate SAM template
          command: |
            cd sam
            sam validate --template-file template.yaml --region "$AWS_REGION"
            sam build
      - run:
          name: Terraform lint & validate
          command: |
            cd terraform/modules/rds
            terraform init -backend=false
            terraform fmt -check
            terraform validate

  deploy_sam:
    executor: python-executor
    steps:
      - checkout
      - install_sam
      - aws-cli/setup
      - run:
          name: Deploy SAM
          command: |
            # Delete existing failed stack if it exists
            aws cloudformation delete-stack --stack-name serverless-rds-automation --region us-east-1 || true
            aws cloudformation wait stack-delete-complete --stack-name serverless-rds-automation --region us-east-1 || true
            cd sam
            sam build
            sam deploy \
              --stack-name serverless-rds-automation \
              --region "$AWS_REGION" \
              --resolve-s3 \
              --no-confirm-changeset \
              --no-fail-on-empty-changeset \
              --capabilities CAPABILITY_IAM \
              --parameter-overrides \
                GitHubToken=$GITHUB_TOKEN \
                GitHubRepo=$GITHUB_REPO \
                StageName=$ENV
            
  terraform_apply:
    executor: python-executor
    steps:
      - checkout
      - install_terraform
      - aws-cli/setup
      - run:
          name: Terraform apply for merged PRs
          command: |
            cd terraform
            terraform init
            terraform workspace select "$ENV" || terraform workspace new "$ENV"
            terraform apply -auto-approve

  cleanup_stack:
    executor: python-executor
    steps:
      - checkout
      - install_sam
      - aws-cli/setup
      - run:
          name: Cleanup SAM stack (scheduled)
          command: |
            if aws cloudformation describe-stacks --stack-name serverless-rds-automation --region "$AWS_REGION" >/dev/null 2>&1; then
              sam delete --stack-name serverless-rds-automation --no-prompts --region "$AWS_REGION"
            else
              echo "Stack not found, skipping"
            fi

workflows:
  version: 2

  validate-every-commit:
    jobs:
      - validate

  deploy-on-main:
    jobs:
      - deploy_sam:
          filters:
            branches:
              only: main
      - terraform_apply:
          requires:
            - deploy_sam
          filters:
            branches:
              only: main

  cleanup-nightly:
    triggers:
      - schedule:
          cron: "0 3 * * *"
          filters:
            branches:
              only: main
    jobs:
      - cleanup_stack